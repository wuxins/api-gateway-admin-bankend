package com.cooljs.modules.[(${module})].mapper[(${subModule}?'.'+${subModule}:'')];

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.cooljs.modules.[(${module})].entity[(${subModule}?'.'+${subModule}:'')].[(${entity})]Entity;

/**
 * [(${name})]
 */
public interface [(${entity})]Mapper extends BaseMapper<[(${entity})]Entity> {
}
